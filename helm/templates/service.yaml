apiVersion: v1
kind: Service
metadata:
  name: main-backend
spec:
  selector:
    app: main-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: LoadBalancer
