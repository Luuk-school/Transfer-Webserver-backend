apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: Transfer-main-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Luuk-school/Transfer-Webserver-backend.git
    targetRevision: HEAD
    path: helm
    helm: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: Transfer-main-backend
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      
    syncOptions:
    - CreateNamespace=true
